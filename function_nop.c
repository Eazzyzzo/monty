#include "monty.h"

/**
	* func_nop - No operation
	* @head: stack head
	* @counter: line_number
	* Return: no return
	*/
void func_nop(stack_t **head, unsigned int counter)
{
	/*These parameters are unused, silence compiler warnings*/
	(void)counter;
	(void)head;
}
